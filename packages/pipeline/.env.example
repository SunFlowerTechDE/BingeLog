# packages/pipeline — the only workspace that knows the service-role key.
# It runs offline and is never part of an app deployment.

SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# M2 onwards. TheTVDB supplies artwork only, never metadata (ADR-002).
TVDB_API_KEY=
TVDB_PIN=
