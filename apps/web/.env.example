# ►►► NUR VORLAGE (.env.example) — HIER NICHTS EINTRAGEN ◄◄◄
# Diese Datei liegt im Repo. Echte Schluessel gehoeren niemals hier hinein.
#
# apps/web — public keys only.
#
# The service-role key must never appear in this workspace (M0 0.2). The
# lint config fails the build if it does.

NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
